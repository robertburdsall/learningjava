public class Stringmethods {

    public static void main(String[] args){

        String name = "Bro";

        //boolean result = name.equals("Bro"); (Returns a boolean if strings equal one another)

        //int result = name.length(); (Returns int that represents length of a string)

        //char result = name.charAt(0); (Finds the character at a given position in a string)

        //int result = name.indexOf("B"); (Finds the index of a given character in a string)

        //boolean result = name.isEmpty(); (Boolean on if a string is empty or not)

        //String result = name.toUpperCase(); (Turns all characters in a string to upper case)

        //String result = name.toLowerCase(); (Turns all characters in a string to lower case)

        //String result = name.trim(); (Removes extra spaces in a string - works with sentances!)

        //String result = name.replace("o","a"); (Replaces a specified character in a string with another specified character)

        //System.out.println(result);
    }
}
